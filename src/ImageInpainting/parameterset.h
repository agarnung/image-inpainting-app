#pragma once

#include <QString>
#include <QList>
#include <QVariant>
#include <QMap>
#include <QVector>

/**
 * @class ParameterSet
 * @brief Stores and manages algorithm parameters.
 *
 * Supports configurable parameters with validation, default values, and dynamic updates.
 */
class ParameterSet
{
    public:
        ParameterSet();
};
